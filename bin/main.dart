import './codewars.dart';

void main(List<String> args) {
  // print(numberToString(number: 20));

  // print(average([2, 3, 4, 4, 5, 2, 3, 4, 5], 5));

  // print(countPositivesSumNegatives(null));

  // print(abbrevName("Sam Harris"));

  // print(evenOrOdd(24));

  // print(smash(['hello', 'world', 'this', 'is', 'great']));

  // print(noSpace("Hello World ! Im Flutter developer !"));

  // print(summation(8));

  // print(grow([1, 2, 3, 4]));

  // print(repeatString(3, "Hello"));

  // print(squareSum([1, 2, 2], 2));

  // print(opposite(-2));
}
